<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp">

    <ImageView
            android:id="@+id/iv_photo_post_item"
            android:layout_width="160dp"
            android:layout_height="222dp"
            android:contentDescription="@string/content_description"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:background="#BDBCBC" />

    <ImageButton
            android:id="@+id/ib_favorite_post_item"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:background="@color/main_screen_image_button_background"
            android:contentDescription="@string/content_description"
            android:src="@drawable/ic_favorite_inactive"
            app:layout_constraintEnd_toEndOf="@id/iv_photo_post_item"
            app:layout_constraintTop_toTopOf="@id/iv_photo_post_item" />

    <TextView
            android:id="@+id/tv_title_post_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:fontFamily="@font/montserrat_medium"
            android:textColor="@color/main_post_item_text_color"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/iv_photo_post_item"
            tools:text="Первый день в Surf" />

</androidx.constraintlayout.widget.ConstraintLayout>